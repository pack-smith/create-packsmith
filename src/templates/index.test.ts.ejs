import { describe, expect, it } from "vitest";
import { PackSmith } from "../src";

describe('PackSmith', () => {
it('should return "Hello PackSmith"', () => {
const result = PackSmith();
expect(result).toBe("Hello PackSmith");
});
});