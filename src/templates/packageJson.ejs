{
"name": "<%= projectName %>",
    "version": "0.0.1",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
    "build": "tsup",
    "lint": "eslint . --ext .ts --ignore-pattern dist/",
    "test": "vitest"
    }
    }